<script type="text/javascript">
  <% if logged_in? %>
    window.currentUser = <%= render("api/users/user.json.jbuilder",
      user: current_user).html_safe %>
  <% end %>

  window.imageAssets = {
    calendarImage:"<%= asset_path("calendar.jpg")%>",
    searchImage:"<%= asset_path("searchIcon.jpg")%>"
  }
</script>

<main id="content"/>
